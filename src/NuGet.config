<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <!-- Scoped to this project so the global NuGet config is left alone. -->
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
  </packageSources>
</configuration>
